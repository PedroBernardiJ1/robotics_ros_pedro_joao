# NOME DO PROJETO

Breve descrição do que este pacote ROS faz e qual foi o objetivo desta entrega.

## 📋 Lista de Atividades
* Atividade 1

## ⚙️ Como executar

1. **Compilação**
Dentro do contêiner Docker, na raiz do `ros_ws`, execute:

```bash
colcon build --packages-select nome_deste_pacote
source install/setup.bash
```

2. **Execução**
Para rodar o nó principal, utilize:

```bash
ros2 run nome_deste_pacote nome_do_executavel
```

**Dependências necessárias:**
* Dependência 1.
